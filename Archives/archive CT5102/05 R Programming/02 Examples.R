`increment<-` <- function(x){
  x<-x+10
}

`test<-` <- function(x, value) {
  x[2] <- value
  x
}

'capitalize<-' <- function(x,value){
  
  
}

`second<-` <- function(x, value) {
  x[2] <- value
  x
}
x <- 1:10
second(x) <- 5